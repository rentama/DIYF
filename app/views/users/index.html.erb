<ul class="media-list">
  <% @users.each do |user| %>
    <li class="media">
      <div class="media-left">
      </div>
     <div class="media-body">
       <%= link_to user_url(user), class: "media-body" do |f| %>
          <h4 class="media-heading">rentama</h4>
          <div class="media">
            <p>
              れんたまです。
            </p>
          </div>
        <% end %>
      </div>
    </li>
  <% end %>
</ul>
